const TelegramChat = require('./TelegramChat').TelegramChat;
const Brand = require('./Brand').Brand;
const Model = require('./Model').Model;
const User = require('./User').User;

module.exports = {
  TelegramChat,
  Brand,
  Model,
  User
};
